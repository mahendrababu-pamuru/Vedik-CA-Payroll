const $=id=>document.getElementById(id);const msg=(text,type='danger')=>{const el=$('loginMessage');el.className=`alert alert-${type}`;el.textContent=text;el.hidden=false};document.addEventListener('DOMContentLoaded',async()=>{if(!window.sb)return msg('Supabase configuration is missing. Update js/config.js.');const {data:{session}}=await sb.auth.getSession();if(session){location.href='dashboard.html';return}$('loginForm').onsubmit=async e=>{e.preventDefault();$('loginBtn').disabled=true;const {error}=await sb.auth.signInWithPassword({email:$('email').value.trim(),password:$('password').value});$('loginBtn').disabled=false;if(error)return msg(error.message);location.href='dashboard.html'};$('forgot').onclick=async e=>{e.preventDefault();const email=$('email').value.trim();if(!email)return msg('Enter your email address first.');const {error}=await sb.auth.resetPasswordForEmail(email,{redirectTo:location.origin+'/index.html'});msg(error?error.message:'Password reset link sent.','info')}});
